<!DOCTYPE html>
<html lang="en">
  <%- include('../../layout/head') %>
  <body>
    <%- include('../../layout/header') %>
    <main>
      <aside class="left">
        <%- include('../../layout/nav') %>
      </aside>
      <section id="hero">
        <h2> Search Results</h2>
        <ul>
          <% recipes.forEach(recipe => { %>  
            <li class="cards">
              <%- include('../../layout/card', {recipe: recipe}) %>

              <ul class="ingredients">
                <% recipe.ingredients.forEach( ingredient =>{ %>
                  <li class="ingredient-li"><%= ingredient %></li>
                <% }); %>
              </ul>

              <ul class="nutrients">
                <li>Total Fat: <%= recipe.total_fat %>g</li>
                <li>Saturated Fat: <%= recipe.saturated_fat %>g</li>
                <li>Total Carbs: <%= recipe.total_carbohydrate %>g</li>
                <li>Dietary Fiber: <%= recipe.dietary_fiber %>g</li>
                <li>Sugars: <%= recipe.sugars %>g</li>
                <li>Protein: <%= recipe.protein %>g</li>
                <li>Cholesterol: <%= recipe.cholesterol %>mg</li>
                <li>Sodium: <%= recipe.sodium %>mg</li>
                <li>Potassium: <%= recipe.potassium %>mg</li>
              </ul>
              <button class="details-button">Details</button>
              <form method="post" action="/recipe" id="add-form">
                <input name="recipe_name" value="<%= recipe.recipe_name %>">
                <input name="url" value="<%= recipe.url %>">
                <input name="source" value="<%= recipe.source %>">
                <input name="image_url" value="<%= recipe.image_url %>">
                <input name="ingredients" value="<%= recipe.ingredients %>">
                <input name="servings" value="<%= recipe.servings %>">
                <input name="calories" value="<%= recipe.calories %>">
                <input name="total_fat" value="<%= recipe.total_fat %>">
                <input name="saturated_fat" value="<%= recipe.saturated_fat %>">
                <input name="cholesterol" value="<%= recipe.cholesterol %>">
                <input name="sodium" value="<%= recipe.sodium %>">
                <input name="total_carbohydrate" value="<%= recipe.total_carbohydrate %>">
                <input name="dietary_fiber" value="<%= recipe.dietary_fiber %>">
                <input name="sugars" value="<%= recipe.sugars %>">
                <input name="protein" value="<%= recipe.protein %>">
                <input name="potassium" value="<%= recipe.potassium %>">
                <input name="health_labels" value="<%= recipe.health_labels %>">
                <input name="diet_labels" value="<%= recipe.diet_labels %>">
                <button type="submit">Add</button>
              </form>
            </li>
          <% }); %>
        </ul>
      </section>
      <aside class="right">
        <p>Right aside.</p>
        <%# include('../../layout/ads') %>
      </aside>
    </main>
    <footer>
      <%# include('../../layout/footer') %>
    </footer>
  </body>
</html>